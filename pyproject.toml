[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyrandom2pm"
dynamic=["version"]

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]
pythonpath = [
  ".", "src",
]
