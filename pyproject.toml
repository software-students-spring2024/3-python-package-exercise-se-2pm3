[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyrandom2pm"

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]
pythonpath = [
  ".", "src",
]